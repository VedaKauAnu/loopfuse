# LoopFuse requirements
# Install order matters: torch first, then triton, then jax

# Core
torch>=2.2.0
triton>=2.3.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# TPU path (install separately)
# pip install 'jax[tpu]' -f https://storage.googleapis.com/jax-releases/libtpu_releases.html
jax>=0.4.25; platform_system != "Linux"

# Optional: for notebook execution
jupyter>=1.0.0
ipykernel>=6.0.0
